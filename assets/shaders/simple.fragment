#version 450 core
precision mediump float;
layout (location = 0) out vec4 frag_color;

in vec2 text_coord;

uniform sampler2D color_texture;

void main() {
    frag_color = vec4(texture(color_texture, text_coord).rgb, 1.0);
}